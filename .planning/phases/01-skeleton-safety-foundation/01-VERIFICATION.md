---
phase: 01-skeleton-safety-foundation
verified: 2026-04-12T02:25:00Z
status: human_needed
score: 11/13 PLAT requirements fully verified programmatically; 2 require human verification (PLAT-04, PLAT-13)
overrides_applied: 0
re_verification:
  previous_status: null
  previous_score: null
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "docker build -t mcpto:test ."
    expected: "Build succeeds on a Linux host and produces an image using python:3.12-slim-bookworm. docker run -i mcpto:test (with MCPTO_GIT_SHA env) starts the stdio transport and answers a JSON-RPC initialize frame."
    why_human: "PLAT-04 requires actually building and running the Dockerfile. The Dockerfile exists, pins the right base image, installs via uv, and sets the stdio default ENTRYPOINT/CMD, but the verifier cannot exercise `docker build` in this environment. A human must run the build + a docker-run smoke test."
  - test: "GitHub Actions CI 9-cell install matrix on a PR"
    expected: "unit-smoke job passes on all 9 cells {ubuntu-latest, macos-latest, windows-latest} × {3.11, 3.12, 3.13}; lint-types passes on Linux/3.12; integration job is skipped via if: false. `uv tool install .` step succeeds on every OS."
    why_human: "PLAT-13 requires the CI matrix to actually run on GitHub Actions runners. The workflow file is structurally correct (3 jobs, matrix.os × matrix.python, pip install + uv tool install both covered, shell: bash on every step) but the verifier cannot drive GitHub Actions. The first main-branch push or PR is the real green signal."
---

# Phase 1: Skeleton & Safety Foundation Verification Report

**Phase Goal:** A pip/uvx/Docker-installable MCP server starts on stdio and Streamable HTTP, answers `initialize`, exposes `mcp_selftest`, and every critical day-one safety pitfall (stdio stdout discipline, redaction, untrusted-content envelope convention, strict JSON schema posture) is enforced before a single Trino-touching line of code lands.

**Verified:** 2026-04-12T02:25:00Z
**Status:** human_needed (automated checks all passed; PLAT-04 Docker build and PLAT-13 GitHub Actions matrix require human drive)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (from ROADMAP Success Criteria)

| #   | Truth (Success Criterion)                                                                                                                                                                                                                                                                                         | Status             | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | SC-1: Installable via `uv tool install` / `uvx` / `pip install` on macOS/Linux/Windows, `mcp_selftest` returns version/transport/echo                                                                                                                                                                              | ? UNCERTAIN (PLAT-01+PLAT-09 implementations verified; PLAT-13 matrix run is human) | `uv sync --all-extras` succeeds; `mcp-trino-optimizer serve --help` displays documented option set; `tests/tools/test_selftest.py` all 4 tests pass including `test_selftest_returns_mandatory_fields` and `test_selftest_echo_round_trip`; `build_app()` registers exactly one tool `mcp_selftest` and schema-lint is clean. `uv tool install .` path is enumerated in `.github/workflows/ci.yml:95-98`. Cross-OS + cross-Python proof is the Actions matrix (human-verified). |
| 2   | SC-2: Stdio `initialize` over JSON-RPC returns only valid frames on stdout; logs appear on stderr as structured JSON with request_id, tool_name, git_sha, package_version, ISO8601 UTC timestamp                                                                                                                  | ✓ VERIFIED         | `tests/smoke/test_stdio_initialize.py::test_stdio_initialize_produces_only_json_rpc_on_stdout` passes. Three-layer stdout discipline wired: (L1) `logging_setup.py` routes structlog to stderr via `_LazyStderrLoggerFactory`; (L2) `safety/stdout_guard.py` `SentinelWriter` + `install_stdout_guard()` installed inside `transports.run_stdio()` AFTER `os.dup(1)`; (L3) smoke test asserts stdout purity. `tests/logging/test_structured_fields.py` passes all 3 mandatory-field checks. |
| 3   | SC-3: Logging of a dict with Authorization / X-Trino-Extra-Credentials / credential.* keys is hard-redacted to [REDACTED]                                                                                                                                                                                          | ✓ VERIFIED         | `logging_setup._redact_processor` + `REDACTION_DENYLIST` frozenset + `_CREDENTIAL_PATTERN` regex; 11 redaction tests in `tests/logging/test_redaction.py` all pass, including case-insensitive, nested, list-of-dicts, SecretStr, credential.* pattern, cookie, x-trino-extra-credentials.                                                                                                                                                                                          |
| 4   | SC-4: Every tool has strict JSON Schema (additionalProperties:false, bounded maxLength, patterns, bounded arrays); every user-origin string output is wrapped via `wrap_untrusted()`                                                                                                                              | ✓ VERIFIED         | `safety/schema_lint.assert_tools_compliant()` called inside `app.build_app()` as a runtime guard — server crashes at startup on violation. 5 schema-lint tests pass, including `test_all_tools_are_schema_compliant` (real build_app), `test_schema_lint_detects_violation`, `test_schema_lint_rejects_missing_max_length`, `test_schema_lint_rejects_missing_additional_properties_false`, `test_schema_lint_rejects_array_without_max_items`. `safety/envelope.wrap_untrusted()` ships with 8 tests including adversarial prompt-injection, newlines, already-json, large string (100k). |
| 5   | SC-5: CI install matrix on {3.11, 3.12, 3.13} × {mac, linux, win} confirms install + initialize round-trip; README has copy-pasteable mcpServers JSON for stdio/HTTP/Docker; CLAUDE.md defines coding rules/DoD/validation/safe-execution                                                                          | ? UNCERTAIN (docs verified; matrix run is human) | 4 `mcpServers` blocks present in README (stdio, Streamable HTTP, Docker); all 5 README docs tests pass (`test_claude_md_exists`, `test_contributing_md_exists`, stdio block, HTTP block, docker block). CI workflow declares matrix `os: [ubuntu-latest, macos-latest, windows-latest]` × `python: ["3.11","3.12","3.13"]` (9 cells) plus lint-types + integration stub. `CONTRIBUTING.md` contains "Coding rules", "Definition of Done", "Safe-execution boundaries" sections. CLAUDE.md exists at repo root. Real green signal from GitHub Actions is the human-verified item. |

**Score:** 3/5 success criteria fully verified programmatically; 2 partially verified (artifacts all present, but full cross-OS/CI and Docker runtime proof requires human-driven CI run and a `docker build`).

### Required Artifacts (merged across 6 plans)

All artifact paths are absolute from repo root `/Users/allen/repo/mcp-trino-optimizer/`.

| Artifact                                              | Expected                                                                                                            | Status      | Details                                                                                                                                                                                                                         |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pyproject.toml`                                      | Build backend + pinned deps (`mcp[cli]>=1.27.0,<2`, `pydantic-settings>=2.13.1`, `structlog>=25.5.0`, `ruff>=0.15.10`, `pytest-asyncio>=1.3.0`, `hatchling>=1.25`) + tool configs | ✓ VERIFIED  | grep confirms every pin present at expected lines. `[project.scripts] mcp-trino-optimizer = "mcp_trino_optimizer.cli:app"` wired. Hatchling version plugin points at `_version.py`.                                           |
| `src/mcp_trino_optimizer/__init__.py`                 | Importable package exposing `__version__`                                                                           | ✓ VERIFIED  | `uv run python -c "import mcp_trino_optimizer; print(mcp_trino_optimizer.__version__)"` prints `0.1.0`.                                                                                                                         |
| `src/mcp_trino_optimizer/_version.py`                 | `__version__ = "0.1.0"`                                                                                             | ✓ VERIFIED  | File exists, 268 bytes.                                                                                                                                                                                                         |
| `src/mcp_trino_optimizer/py.typed`                    | PEP 561 marker (empty)                                                                                              | ✓ VERIFIED  | 0-byte file exists.                                                                                                                                                                                                             |
| `src/mcp_trino_optimizer/safety/envelope.py`          | `wrap_untrusted()` returns exact `{"source": "untrusted", "content": s}` TypedDict                                  | ✓ VERIFIED  | 1216 bytes; mypy strict clean; 8 tests pass including adversarial prompt injection, control characters, large string near-cap.                                                                                                  |
| `src/mcp_trino_optimizer/safety/stdout_guard.py`      | `SentinelWriter` + `install_stdout_guard()` / `uninstall_stdout_guard()`, idempotent, lazy logging_setup import     | ✓ VERIFIED  | 3700 bytes; `class SentinelWriter`, `fileno()` raises OSError, lazy `from mcp_trino_optimizer.logging_setup import get_logger` inside `write`. 6 tests pass.                                                                     |
| `src/mcp_trino_optimizer/safety/schema_lint.py`       | `assert_tools_compliant(mcp)` + `SchemaLintError` + `_check_schema`; constants `MAX_STRING_LEN=100_000`, `MAX_ARRAY_LEN=1_000`, `MAX_PROSE_LEN=4_096` | ✓ VERIFIED  | 5174 bytes; recurses into `$defs`/`definitions`/`anyOf`/`oneOf`/`allOf`; reads `mcp._tool_manager.list_tools()`. Root-schema `additionalProperties` check deliberately loosened with documented rationale at lines 66-75. 5 tests pass. |
| `src/mcp_trino_optimizer/settings.py`                 | `Settings(BaseSettings)` + `load_settings_or_die()`, `env_prefix="MCPTO_"`, `extra="forbid"`, fail-fast on http without bearer | ✓ VERIFIED  | 3261 bytes; `model_validator(mode="after")` enforces bearer requirement; `load_settings_or_die` emits structured JSON error to stderr via orjson and `sys.exit(2)`. 8 tests pass. Spot-check: `mcp-trino-optimizer serve --transport http` exits 2 with `{"level":"error","event":"settings_error",...}` on stderr. |
| `src/mcp_trino_optimizer/logging_setup.py`            | `configure_logging()` + `get_logger()` + `REDACTION_DENYLIST` + `_redact_processor`; stderr-only; injects package_version/git_sha | ✓ VERIFIED  | 5614 bytes; `_LazyStderrLoggerFactory` resolves `sys.stderr` per logger for pytest capsys compatibility; processor order is LOAD-BEARING as documented (merge_contextvars → level → timestamp → static fields → redact → exc → render). 3 structured-field tests + 11 redaction tests pass. |
| `src/mcp_trino_optimizer/_context.py`                 | `new_request_id()` + contextvar helpers for request_id                                                              | ✓ VERIFIED  | 808 bytes; referenced by `tools/_middleware.py` at line 20.                                                                                                                                                                     |
| `src/mcp_trino_optimizer/_runtime.py`                 | `RuntimeInfo` dataclass + `runtime_info()` + `_resolve_git_sha()` 3-tier fallback + `set_transport()`               | ✓ VERIFIED  | 2808 bytes; 4-tier fallback (env MCPTO_GIT_SHA → baked `_git_sha.txt` → `git rev-parse HEAD` 1s timeout → `"unknown"`); `_resolve_git_sha` never raises (all except paths caught).                                              |
| `src/mcp_trino_optimizer/app.py`                      | `build_app()` constructs FastMCP, discover_and_register tools, assert_tools_compliant                               | ✓ VERIFIED  | 2103 bytes; calls `tools.discover_and_register(mcp)` then `assert_tools_compliant(mcp)`. Spot-check `uv run python -c "...build_app()..."` succeeds, logs `tools_registered count=1`, lists `['mcp_selftest']`.                 |
| `src/mcp_trino_optimizer/tools/__init__.py`           | `discover_and_register(mcp)` walks sibling modules, calls `register(mcp)` entry point                               | ✓ VERIFIED  | 1657 bytes; `pkgutil.iter_modules(__path__)` + `importlib.import_module` + skips `_middleware` via `name.startswith("_")`.                                                                                                      |
| `src/mcp_trino_optimizer/tools/selftest.py`           | `mcp_selftest` tool returning server_version/transport/echo/python_version/package_version/git_sha/log_level/started_at/capabilities, wrapped by `tool_envelope` | ✓ VERIFIED  | 3500 bytes; `SelftestOutput` BaseModel with `ConfigDict(extra="forbid", json_schema_extra={"additionalProperties": False})`; `@tool_envelope("mcp_selftest")` binds request_id + tool_name. 4 selftest tests pass.              |
| `src/mcp_trino_optimizer/tools/_middleware.py`        | `tool_envelope(tool_name)` decorator binding request_id + tool_name contextvars                                     | ✓ VERIFIED  | 1657 bytes; uses `new_request_id()` from `_context`, `structlog.contextvars.bound_contextvars`.                                                                                                                                 |
| `src/mcp_trino_optimizer/transports.py`               | `run_stdio()` (duped fd + SentinelWriter), `run_streamable_http()` (uvicorn), `StaticBearerMiddleware` (hmac.compare_digest) | ✓ VERIFIED  | 6415 bytes; `os.dup(1)` + `anyio.wrap_file` + `stdio_server(stdout=...)` + `install_stdout_guard()`; HTTP path emits `plaintext_http_warning`; `StaticBearerMiddleware.dispatch` uses `hmac.compare_digest`. 4 HTTP bearer tests + stdio smoke test pass. |
| `src/mcp_trino_optimizer/cli.py`                      | Typer app with `serve` subcommand, `--transport/--host/--port/--log-level/--bearer-token`                           | ✓ VERIFIED  | 3456 bytes; belt-and-suspenders `logging.basicConfig(stream=sys.stderr, force=True)` at module top; `@app.callback()` no-op so subcommand name is not swallowed. `mcp-trino-optimizer serve --help` output matches PLAN exactly. |
| `Dockerfile`                                          | Multi-stage build on `python:3.12-slim-bookworm`, `uv pip install`, stdio default                                   | ✓ VERIFIED (static) / ⚠️ HUMAN (runtime) | Lines 4 & 25 use `python:3.12-slim-bookworm`; line 18 `uv pip install --no-cache .`; line 40 `ENTRYPOINT ["mcp-trino-optimizer", "serve"]`; line 41 `CMD ["--transport", "stdio"]`. Actual `docker build` not run — human verification item. |
| `.dockerignore`                                       | Excludes .venv, .git, tests, __pycache__                                                                            | ✓ VERIFIED  | File exists (379 bytes).                                                                                                                                                                                                        |
| `README.md`                                           | Full README with copy-pasteable mcpServers JSON for stdio, Streamable HTTP, Docker                                  | ✓ VERIFIED  | 4 `mcpServers` occurrences; 3 transport blocks (stdio, HTTP, Docker). 5 README docs tests pass.                                                                                                                                 |
| `CONTRIBUTING.md`                                     | Coding rules, Definition of Done, safe-execution boundaries                                                         | ✓ VERIFIED  | 4636 bytes; sections `## Coding rules`, `## Definition of Done`, `## Safe-execution boundaries` present.                                                                                                                        |
| `.env.example`                                        | Documents every MCPTO_ env var with safe example value; no real secrets                                             | ✓ VERIFIED  | Declares MCPTO_TRANSPORT, MCPTO_HTTP_HOST, MCPTO_HTTP_PORT, MCPTO_HTTP_BEARER_TOKEN (with CHANGE_ME placeholder), MCPTO_LOG_LEVEL, MCPTO_GIT_SHA; Trino-side vars documented as Phase 2+ placeholders.                           |
| `LICENSE`                                             | Apache-2.0 text                                                                                                     | ✓ VERIFIED  | 11075 bytes.                                                                                                                                                                                                                    |
| `.gitattributes`                                      | CRLF safety                                                                                                         | ✓ VERIFIED  | 556 bytes.                                                                                                                                                                                                                      |
| `.github/workflows/ci.yml`                            | 3 jobs (lint-types, unit-smoke 9-cell, integration stub); `shell: bash` on every step; both `pip install -e` and `uv tool install .` paths | ✓ VERIFIED (static) / ⚠️ HUMAN (runtime) | lint-types: 1 cell (ubuntu/3.12) ruff format + ruff check + mypy; unit-smoke: matrix `os × python` with `uv pip install -e .[dev]`, pytest, stdio smoke, HTTP bearer smoke, CLI help, `uv tool install .`; integration: `if: false` stub. Every step has `shell: bash`. Actual matrix run on Actions required for PLAT-13 close. |
| `.pre-commit-config.yaml`                             | ruff + mypy hooks                                                                                                   | ✓ VERIFIED  | `ruff-check`, `ruff-format`, `mypy` hooks from astral-sh/ruff-pre-commit and mirrors-mypy.                                                                                                                                      |
| `tests/` tree                                         | conftest + 16 test files (smoke, logging, tools, safety, docs, settings)                                            | ✓ VERIFIED  | `uv run pytest -q` → 61 passed, 0 failed, 0 skipped, 0 xfailed. All stub files from VALIDATION.md Wave 0 flipped green.                                                                                                         |

**Summary:** 26/26 artifacts verified at code level. 2 artifacts (`Dockerfile`, `.github/workflows/ci.yml`) require human verification of actual Docker build / Actions matrix runtime behavior because the verifier can't `docker build` or drive GitHub Actions in this environment.

### Key Link Verification

| From                                          | To                                                             | Via                                                                                         | Status    | Details                                                                                                                                     |
| --------------------------------------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `pyproject.toml`                              | `src/mcp_trino_optimizer/_version.py`                          | `[tool.hatch.version].path`                                                                 | ✓ WIRED   | `uv sync --all-extras` + `import mcp_trino_optimizer; __version__ == "0.1.0"` confirms hatch reads the version plugin.                      |
| `pyproject.toml`                              | pytest                                                         | `[tool.pytest.ini_options] asyncio_mode = "auto"`                                           | ✓ WIRED   | 61 async+sync tests run under `mode=Mode.AUTO` per pytest output.                                                                           |
| `safety/schema_lint.py`                       | `mcp._tool_manager.list_tools()`                               | private SDK attribute                                                                       | ✓ WIRED   | Line 43 `mcp._tool_manager`; `test_all_tools_are_schema_compliant` passes on real `build_app()`.                                            |
| `safety/stdout_guard.py`                      | `logging_setup.get_logger`                                     | lazy import inside `SentinelWriter.write`                                                   | ✓ WIRED   | Line 47 `from mcp_trino_optimizer.logging_setup import get_logger` inside `write()`; fallback JSON-to-stderr path exists.                   |
| `app.py`                                      | `safety/schema_lint.py`                                        | `assert_tools_compliant(mcp)` call after registration                                       | ✓ WIRED   | `app.py:46` — runtime guard crashes server on schema violation.                                                                             |
| `transports.py`                               | `safety/stdout_guard.py`                                       | `install_stdout_guard()` inside `run_stdio` after `os.dup(1)`                               | ✓ WIRED   | `transports.py:57-67` — fd dup, install guard, set transport, then `anyio.run(_run)`.                                                       |
| `transports.py`                               | `hmac.compare_digest`                                          | `StaticBearerMiddleware.dispatch` constant-time compare                                     | ✓ WIRED   | `transports.py:115` — `hmac.compare_digest(presented, self._token_bytes)`.                                                                  |
| `cli.py`                                      | `settings.py`                                                  | `load_settings_or_die(**overrides)`                                                         | ✓ WIRED   | `cli.py:72`. Spot-check: `serve --transport http` exits 2 with settings_error JSON.                                                         |
| `tools/__init__.py`                           | every sibling module                                           | `pkgutil.iter_modules` + `importlib.import_module` + `module.register(mcp)`                 | ✓ WIRED   | `build_app()` logs `tools_registered count=1`; `mcp_selftest` appears in `mcp._tool_manager.list_tools()`.                                   |
| `tools/selftest.py`                           | `mcp.tool()` decorator                                         | `register(mcp)` entry point                                                                 | ✓ WIRED   | `selftest.py:49-93` — `def register(mcp)` calls `@mcp.tool(...)`.                                                                           |
| `settings.py`                                 | `pydantic_settings.BaseSettings`                               | `env_prefix="MCPTO_"`, `env_file=".env"`, `extra="forbid"`                                  | ✓ WIRED   | `settings.py:25-31`.                                                                                                                        |
| `logging_setup.py`                            | `sys.stderr`                                                   | `_LazyStderrLoggerFactory` resolving stderr per logger                                      | ✓ WIRED   | `logging_setup.py:105-118` — documented rationale for per-call stderr resolution (capsys compatibility).                                    |
| `logging_setup.py`                            | SecretStr rendering                                            | `_redact_processor` isinstance check                                                        | ✓ WIRED   | `logging_setup.py:61` — `isinstance(obj, SecretStr)` → `"[REDACTED]"`. Confirmed by `test_secretstr_redacted`.                              |
| `.github/workflows/ci.yml`                    | `tests/smoke/test_stdio_initialize.py`                         | Named step "Stdio cleanliness smoke test (PLAT-05)"                                         | ✓ WIRED   | `.github/workflows/ci.yml:76-81`.                                                                                                           |
| `Dockerfile`                                  | `pyproject.toml`                                               | `uv pip install --no-cache .` from copied files                                             | ✓ WIRED   | `Dockerfile:18` inside builder stage; actual build execution is human-verification.                                                          |

**Summary:** 15/15 key links wired. All critical connections (private-attr SDK access, lazy-import circular break, stdout-guard ordering, bearer constant-time compare, settings fail-fast, tool auto-discovery) are verified against live code and tests.

### Data-Flow Trace (Level 4)

| Artifact                  | Data Variable                      | Source                                                                 | Produces Real Data | Status    |
| ------------------------- | ---------------------------------- | ---------------------------------------------------------------------- | ------------------ | --------- |
| `tools/selftest.py`       | `info = runtime_info()`            | `_runtime.runtime_info()` → `importlib.metadata.version`, `sys.version`, git sha fallback chain | Yes                | ✓ FLOWING |
| `transports.py`           | `presented` bearer                 | `request.headers["authorization"]` parsed from real Starlette Request  | Yes                | ✓ FLOWING |
| `logging_setup.py`        | `package_version`, `git_sha`       | `cli.py` reads `runtime_info()` before calling `configure_logging()`   | Yes                | ✓ FLOWING |
| `cli.py`                  | `settings`                         | `load_settings_or_die()` reads env → .env → CLI overrides              | Yes                | ✓ FLOWING |
| `app.py`                  | tool list                          | `tools.discover_and_register(mcp)` (live pkgutil walk)                 | Yes (1 tool)       | ✓ FLOWING |

No HOLLOW artifacts. The selftest tool returns real runtime data, not hardcoded placeholders; the bearer middleware validates against a real header; schema_lint walks a real FastMCP instance with a real registered tool.

### Behavioral Spot-Checks

| Behavior                                                  | Command                                                                        | Result                                                                   | Status  |
| --------------------------------------------------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------ | ------- |
| Package imports and exposes __version__                   | `uv run python -c "import mcp_trino_optimizer; print(mcp_trino_optimizer.__version__)"` | `0.1.0`                                                                  | ✓ PASS  |
| CLI entry point responds to --help                        | `uv run mcp-trino-optimizer serve --help`                                      | Documented `--transport / --host / --port / --log-level / --bearer-token` | ✓ PASS  |
| HTTP fail-fast without bearer token                       | `uv run mcp-trino-optimizer serve --transport http; echo $?`                   | stderr JSON `event: settings_error`, exit code `2`                       | ✓ PASS  |
| Full test suite green                                     | `uv run pytest -q`                                                             | `61 passed`                                                              | ✓ PASS  |
| Stdio JSON-RPC initialize smoke                           | `uv run pytest tests/smoke/test_stdio_initialize.py -v`                        | 1 passed — stdout purity enforced                                        | ✓ PASS  |
| HTTP bearer middleware (missing / wrong / correct token)  | `uv run pytest tests/smoke/test_http_bearer.py -v`                             | 4 passed                                                                 | ✓ PASS  |
| build_app() constructs and registers tools                | `uv run python -c "from mcp_trino_optimizer.app import build_app; m = build_app(); print([t.name for t in m._tool_manager.list_tools()])"` | logs `tools_registered count=1`, prints `['mcp_selftest']`              | ✓ PASS  |
| Ruff format + ruff check + mypy strict                    | `uv run ruff format --check . && uv run ruff check . && uv run mypy src`       | All passed; 34 files formatted; 16 source files strict-typed             | ✓ PASS  |
| Docker image builds and runs                              | `docker build -t mcpto:test . && docker run -i mcpto:test`                     | SKIPPED (no docker in verifier env)                                      | ? SKIP  |
| CI matrix runs on all 9 cells                             | Actions PR run                                                                 | SKIPPED (no Actions drive in verifier env)                               | ? SKIP  |

### Requirements Coverage (PLAT-01 through PLAT-13)

| Requirement | Source Plan(s)          | Description                                                                                                         | Status                 | Evidence                                                                                                                                                                                                                                         |
| ----------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PLAT-01     | 01-01, 01-06            | Install via `uv tool install` / `uvx` / `pip install` on mac/linux/win                                               | ✓ SATISFIED (code + CI) / ? NEEDS HUMAN (full matrix run) | pyproject.toml declares console-script; `uv tool install .` step in CI at line 94-98; pip install path at line 63-67. Cross-OS proof requires Actions run. |
| PLAT-02     | 01-01, 01-04            | Stdio transport answers JSON-RPC initialize; mcpServers block discoverable                                           | ✓ SATISFIED            | `tests/smoke/test_stdio_initialize.py` passes; `transports.run_stdio()` wired with fd dup + SentinelWriter; README stdio mcpServers block at README.md line 57.                                                                                    |
| PLAT-03     | 01-01, 01-04            | Streamable HTTP on 127.0.0.1 with static bearer, fail-fast, hmac compare                                            | ✓ SATISFIED            | 4 `tests/smoke/test_http_bearer.py` tests pass; `StaticBearerMiddleware` uses `hmac.compare_digest`; Settings `_require_bearer_for_http` validator; `load_settings_or_die` exits 2 with structured stderr JSON (spot-checked).                     |
| PLAT-04     | 01-05                   | Docker image on `python:3.12-slim-bookworm` base, stdio default                                                     | ⚠️ NEEDS HUMAN         | Dockerfile lines 4, 25, 40, 41 satisfy static contract; actual `docker build` not executed in verifier env.                                                                                                                                        |
| PLAT-05     | 01-01, 01-02, 01-04     | stdout discipline — logs to stderr only; CI smoke asserts pure JSON-RPC on stdout                                   | ✓ SATISFIED            | Three layers: (L1) `logging_setup` stderr-only; (L2) `SentinelWriter` + `install_stdout_guard()` installed inside `run_stdio()`; (L3) `tests/smoke/test_stdio_initialize.py` passes and is a named CI step.                                        |
| PLAT-06     | 01-01, 01-03            | Structured JSON with request_id, tool_name, git_sha, package_version, ISO8601 UTC timestamp                         | ✓ SATISFIED            | `logging_setup.configure_logging` injects package_version + git_sha via lambda processor; ISO8601 via `TimeStamper(fmt="iso", utc=True)`; request_id + tool_name bound via contextvars inside `tool_envelope`. 3 structured-field tests pass. |
| PLAT-07     | 01-01, 01-03            | Redact Authorization / X-Trino-Extra-Credentials / credential.* to [REDACTED]                                       | ✓ SATISFIED            | 11 redaction tests pass; `REDACTION_DENYLIST` frozenset + `_CREDENTIAL_PATTERN` regex; SecretStr isinstance redaction; recursive walk through dict/list/tuple.                                                                                     |
| PLAT-08     | 01-01, 01-03            | pydantic-settings, env > file > default precedence, SecretStr for secrets                                            | ✓ SATISFIED            | 8 settings tests pass including precedence, extra=forbid, port range, http-without-bearer fail-fast, load_settings_or_die exits with structured JSON. `http_bearer_token: SecretStr | None`.                                                         |
| PLAT-09     | 01-01, 01-04            | `mcp_selftest` returns server version, transport, capabilities, echo                                                | ✓ SATISFIED            | 4 selftest tests pass; `SelftestOutput` has server_version, transport, echo, python_version, package_version, git_sha, log_level, started_at, capabilities. `@tool_envelope` binds request_id + tool_name.                                         |
| PLAT-10     | 01-01, 01-02, 01-04     | Strict JSON Schemas — additionalProperties:false, maxLength, pattern, maxItems                                      | ✓ SATISFIED            | 5 schema_lint tests pass; runtime guard `assert_tools_compliant(mcp)` called inside `build_app()`; MAX_STRING_LEN=100_000 SQL cap; `test_all_tools_are_schema_compliant` verifies real registered `mcp_selftest`.                                   |
| PLAT-11     | 01-01, 01-02            | `wrap_untrusted()` returns exact `{"source": "untrusted", "content": s}` envelope                                    | ✓ SATISFIED            | 8 envelope tests pass including adversarial prompt injection (`<|im_start|>`, triple backticks), control chars, 100k string.                                                                                                                       |
| PLAT-12     | 01-01, 01-05            | README with copy-pasteable mcpServers JSON blocks for stdio/HTTP/Docker; CLAUDE.md coding rules, DoD, safe-exec     | ✓ SATISFIED            | 5 docs tests pass; 4 `mcpServers` blocks in README; CONTRIBUTING.md has Coding rules / Definition of Done / Safe-execution boundaries sections.                                                                                                    |
| PLAT-13     | 01-01, 01-06            | CI install-matrix on 3.11/3.12/3.13 × mac/linux/win                                                                 | ⚠️ NEEDS HUMAN         | Workflow declares 9-cell matrix with both pip install and uv tool install paths; `shell: bash` on every step; Stdio cleanliness + HTTP bearer as named steps. Actual Actions run required for full close.                                          |

**Orphaned requirements:** None. Every PLAT-01..PLAT-13 requirement is claimed by at least one plan and has corresponding implementation + test evidence.

**Requirements summary:** 11/13 SATISFIED programmatically; 2/13 (PLAT-04, PLAT-13) NEEDS HUMAN for runtime proof of operations the verifier can't execute (docker build, GitHub Actions driver).

### Anti-Patterns Found

Scanned all files in `src/mcp_trino_optimizer/` tree:

| File                                              | Line | Pattern                                             | Severity | Impact                                                                                                                                                                                                                                   |
| ------------------------------------------------- | ---- | --------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `safety/schema_lint.py`                           | 66-75 | Root-level `additionalProperties` check skipped     | ℹ️ Info  | Deliberately loosened with extensive rationale comment: FastMCP auto-generates the `{tool}Arguments` wrapper and does not emit `additionalProperties: false` on it. Parameter-name mismatches at the root are caught by pydantic validation. Documented and consistent with test expectations. |
| `safety/stdout_guard.py`                          | 54   | Broad `except Exception`                            | ℹ️ Info  | Intentional fallback-must-not-raise path with inline justification comment. Fallback writes structured JSON to stderr so the event is never lost.                                                                                       |
| `settings.py`                                     | 78   | `except ValidationError as e`                       | ℹ️ Info  | Narrow catch on pydantic-specific exception; correct fail-fast handling.                                                                                                                                                                |
| `_runtime.py`                                     | 61   | `except (FileNotFoundError, ...)`                   | ℹ️ Info  | Specific exception tuple; `_resolve_git_sha` must never raise by contract (documented in module docstring).                                                                                                                             |

**No blockers, no warnings.** All flagged patterns are intentional and documented at the point of use. No TODO/FIXME/placeholder/stub content in `src/`. No empty-handler anti-patterns. No hardcoded empty data flowing to user-visible output. No `return null` / `return []` shortcuts in runtime code.

### Human Verification Required

Two items remain that require human execution because the verifier cannot drive docker or GitHub Actions in this environment.

#### 1. Docker build + stdio smoke (PLAT-04)

**Test:**
```bash
cd /Users/allen/repo/mcp-trino-optimizer
docker build -t mcpto:test .
# Then inside a temporary container, confirm the CLI launches on stdio by default:
printf '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-03-26","capabilities":{},"clientInfo":{"name":"manual","version":"0.0.0"}}}\n' | \
  docker run --rm -i -e MCPTO_GIT_SHA=manual-test mcpto:test
```

**Expected:**
- `docker build` succeeds without errors on a Linux host with docker installed
- Image uses `python:3.12-slim-bookworm` base (confirm via `docker image inspect`)
- `docker run -i mcpto:test` (no args) starts stdio transport and responds to the `initialize` frame on stdout with a JSON-RPC response
- Log output on stderr is structured JSON

**Why human:** The verifier can statically inspect the Dockerfile (all contents match PLAN spec — lines 4/25 use the correct base, line 18 uses `uv pip install`, lines 40-41 set the stdio default ENTRYPOINT/CMD) but cannot execute `docker build` inside this environment. This is the one remaining proof point for PLAT-04.

#### 2. CI install-matrix on GitHub Actions (PLAT-13)

**Test:**
- Push the current HEAD (or open a PR from a feature branch) to `main` on the GitHub repository hosting this project
- Watch the CI workflow run

**Expected:**
- `lint-types` job passes on ubuntu-latest / Python 3.12 (ruff format + ruff check + mypy strict)
- `unit-smoke` matrix passes on all 9 cells: `{ubuntu-latest, macos-latest, windows-latest} × {3.11, 3.12, 3.13}` — each cell running pytest, stdio smoke, HTTP bearer smoke, `mcp-trino-optimizer --help`, and `uv tool install .`
- `integration` job is skipped (`if: false`)
- Both install paths exercised: `uv pip install -e .[dev]` (step "Install package") and `uv tool install .` (step "Verify `uv tool install .` path")

**Why human:** The workflow file is structurally complete and lints cleanly, but the verifier cannot drive GitHub Actions runners. `pytest` running locally on macOS passes 61/61 and provides strong evidence the tests are well-behaved, but cross-platform proof (Windows line endings in stdio framing, `uv tool install` PATH behavior, macOS subprocess semantics) is only obtained from a real matrix run.

### Gaps Summary

No gaps that block the phase goal. The implementation delivers every PLAT requirement with concrete artifacts, passing tests, and verified key links. 11 of 13 PLAT IDs are fully SATISFIED by programmatic checks; 2 (PLAT-04 Docker runtime, PLAT-13 CI matrix runtime) are fully SATISFIED at the artifact level but require human execution of operations outside the verifier's capability (docker build, Actions driver) to close the contract end-to-end.

**What's notably strong:**
- Three-layer stdout discipline is genuinely three-layer — logging_setup enforces stderr only, stdout_guard replaces sys.stdout with SentinelWriter, and the smoke test asserts stdout purity from a spawned subprocess
- Schema-lint runtime guard in `build_app()` complements the CI test with the same code path, so a regression is caught both at CI time and startup time
- Settings fail-fast is spot-checked working: `serve --transport http` exits 2 with a structured JSON error on stderr before any port binds
- `_LazyStderrLoggerFactory` documented rationale is subtle but correct — it preserves pytest capsys fidelity without cost in production
- HTTP bearer middleware uses `hmac.compare_digest` (constant-time) and is tested for missing/wrong/correct token cases
- tools auto-discovery pattern via `register(mcp)` entry point correctly avoids the module-level `@mcp.tool()` circular-import hazard documented in the RESEARCH notes

**What's intentionally loosened (documented):**
- `schema_lint._check_schema` skips the root-level `additionalProperties` check because FastMCP auto-generates the wrapper and parameter-name validation is handled by pydantic (lines 66-75 of `safety/schema_lint.py` carry the full rationale)
- `pytest.ini_options` does not set `filterwarnings = ["error"]` (PLAN 01-01 explicitly defers this to avoid blocking Wave 0 stubs)

---

_Verified: 2026-04-12T02:25:00Z_
_Verifier: Claude (gsd-verifier)_
